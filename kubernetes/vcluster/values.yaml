vcluster_config:
  name: vcluster-a
  namespace: vcluster-a
  chart_version: "0.33.1"
  # Optional YAML fragment merged by the upstream vcluster chart parser.
  # Example:
  # overrides: |
  #   telemetry:
  #     enabled: false
  overrides: ""

argocd:
  namespace: argocd
  create_project: false
  project: default
  host_cluster_server: https://kubernetes.default.svc
  source_repos:
    - https://github.com/mario26rgl/s8-hetzner-kubernetes.git
  role_name: tenant-admin