name: Build and Push Broker Service Container

on:
  push:
    branches: [ main ]
    paths:
      - code/broker-service/**
      - .github/workflows/build-broker-service.yaml

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: broker-service

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      digest: ${{ steps.digest.outputs.value }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build broker binary
        run: |
          cd code/broker-service
          CGO_ENABLED=0 GOOS=linux go build -o brokerApp ./cmd/api

      - name: Build and push Broker Service Container
        uses: macbre/push-to-ghcr@master
        with:
          image_name: mario26rgl/${{ env.IMAGE_NAME }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dockerfile: ./code/broker-service/broker.dockerfile
          context: ./code/broker-service
          image_tag: ${{ github.sha }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Get image digest
        id: digest
        run: |
          DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' \
            ghcr.io/mario26rgl/broker-service:${{ github.sha }} | cut -d@ -f2)
          echo "value=$DIGEST" >> $GITHUB_OUTPUT

      - name: Sign the image
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
        run: |
          cosign sign --key env://COSIGN_PRIVATE_KEY \
            --tlog-upload=false \
            --yes \
            ghcr.io/mario26rgl/broker-service@${{ steps.digest.outputs.value }}

  update-manifest:
    runs-on: ubuntu-latest
    needs: build-and-push
    permissions:
      contents: write
    concurrency:
      group: update-manifest
      cancel-in-progress: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync latest main (pick up other workflows' commits)
        run: git pull --rebase origin main

      - name: Update Helm chart with new Docker image tag
        run: |
          sed -i "s|image: ghcr.io/mario26rgl/broker-service:.*|image: ghcr.io/mario26rgl/broker-service@${{ needs.build-and-push.outputs.digest }}|" \
            kubernetes/apps/broker.yaml
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add kubernetes/apps/broker.yaml
          git commit -m "chore: update broker-service image digest to ${{ github.sha }}"
          git push origin main
